# You can either copy this file to .env manually or fill values in the app's
# Settings page. Never commit real API keys.

OPENAI_COMPATIBLE_BASE_URL=
OPENAI_COMPATIBLE_API_KEY=
OPENAI_COMPATIBLE_MODEL=

# Optional: override local SQLite location.
# APP_DB_PATH=data/app.sqlite3
