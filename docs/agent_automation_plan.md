# Agent 自动化路线

更新时间：2026-08-06

## 1. 最终目标

本项目最终目标不是一个固定脚本，而是一个受控自动化求职 Agent。

理想工作流：

1. 用户点击“寻找岗位”。
2. Agent 在指定招聘平台搜索 AI 应用开发、Agent 开发、AI 后端等相关实习岗位。
3. Agent 采集候选岗位，去重，读取 JD，做岗位匹配评分和公司风险分析。
4. 低匹配或高风险岗位自动归档，并保存跳过原因。
5. 中间分数岗位保存为“待用户判断”，并生成建议询问点。
6. 高匹配、低风险岗位进入沟通候选。
7. 在允许的沟通模式下，Agent 可以向 HR 询问岗位相关信息，保存回复并重新评分。
8. 对方愿意继续且岗位仍高匹配时，Agent 整理投递理由和推荐简历版本。
9. 发送简历、上传附件、点击投递永远需要用户确认。
10. HR 提到面试后，Agent 标记为“面试邀请”；用户确认面试时间和形式后手动改为“待面试”，Agent 再开始面试准备。

## 2. 为什么不是脚本

脚本适合固定页面和固定按钮流程，但招聘平台存在登录、验证码、反爬、页面结构变化、HR 自然语言回复等不确定因素。

本项目需要的是：

- 状态机：每个岗位都有明确阶段和下一步动作。
- 工具层：浏览器扩展、Playwright、LLM、搜索、数据库分别承担不同职责。
- 决策层：规则先筛，LLM 做理解和生成，关键动作由用户确认。
- 审计日志：每一步搜索、评分、沟通、跳过和错误都可追踪。
- 安全边界：不托管密码，不绕过验证码，不自动发送敏感信息。

## 3. 系统架构

### 3.1 本地控制中心

现有 FastAPI + Jinja2 + SQLite 继续作为本地控制中心：

- 保存候选人画像、简历版本、岗位、候选岗位、沟通日志、面试准备和模型调用日志。
- 提供本地网页界面。
- 提供浏览器扩展调用的本地 API。
- 执行 JD 分析、评分、风险判断、话术生成和面试准备。

### 3.2 浏览器扩展

浏览器扩展是中期主路线。

用途：

- 运行在用户正常登录的 Edge/Chrome 中。
- 采集当前岗位详情页。
- 采集当前搜索结果页。
- 后续采集当前 HR 对话页。
- 把采集结果发送给本地应用。

边界：

- 不读取 Cookie、密码、验证码、请求头、本地存储或隐藏字段。
- 不截图。
- 不保存整页 HTML。
- 第一版不自动发消息、不自动投递。

### 3.3 Playwright 自动化

Playwright 用于长期受控自动化。

用途：

- 在独立的受控浏览器 profile 中自动搜索、打开岗位、读取消息、填写草稿和执行已确认动作。
- 用户需要为该 profile 登录招聘平台一次。
- 自动化 profile 不提交、不导出、不写入数据库。

边界：

- 不强行接管用户平时正在使用的普通 Edge。
- 不绕过验证码。
- 不在未确认时上传简历或点击投递。

### 3.4 LLM 决策层

LLM 负责理解、判断、生成和复盘，不直接操作网页。

LLM 输出结构化决策，例如：

```json
{
  "action": "ask_hr",
  "reason": "岗位匹配较高，但 JD 没写实习周期和到岗要求。",
  "message": "您好，想请问这个岗位的主要工作内容、技术栈和每周到岗要求是怎样的？"
}
```

本地策略层再判断该动作能否执行。

## 4. 平台和搜索范围

第一批平台：

- Boss 直聘
- 猎聘
- 实习僧
- 智联招聘
- 前程无忧

搜索关键词可由 Agent 自动扩展，并保存每次实际使用的搜索词。

默认关键词方向：

- AI 应用开发实习
- 大模型应用开发实习
- Agent 开发实习
- RAG 开发实习
- Python AI 后端实习
- AI 后端实习

城市优先级：

- 杭州
- 深圳
- 广州
- 上海
- 北京
- 成都
- 重庆

远程、线下、混合办公均可接受。

去重规则：

- 公司名 + 岗位名 + 平台 + 链接。
- 同公司同岗位跨平台保留，但标记为“疑似重复”。

默认搜索上限：

- 每个平台最多采集 20 个候选。
- 每轮总候选最多 100 个。
- 上限后续可在设置中调整。

## 5. 岗位评分和分档

前 2-3 轮搜索为观察期：

- 只采集、评分、统计分数分布。
- 不自动发消息。
- 不固定高/中/低阈值。
- 用真实岗位结果校准评分模型和分档线。

评分维度：

- 技术匹配
- 成长性
- 薪资
- 地点与时间
- 公司风险
- 岗位真实性 / 招聘认真程度

岗位真实性重点判断：

- JD 是否具体。
- 是否包含真实工程内容。
- 是否像技术岗位，而不是包装成 AI 的销售、运营、培训或标注。
- 公司招聘行为是否可疑。

分档动作：

- 高匹配、低风险：进入沟通候选。
- 中间分数：保存为“待用户判断”，可生成建议询问点但不发送。
- 低匹配或高风险：自动归档，并保存跳过原因。

低分跳过原因必须保存，例如：

- 技术栈不匹配。
- 偏销售、运营、客服、标注或培训。
- 薪资、城市或实习时间不合适。
- 公司信息不足。
- 命中培训费、贷款、押金等风险信号。

## 6. 自动沟通模式

只保留三个模式。

### 6.1 关闭

什么都不做：

- 不监听。
- 不分析 HR 回复。
- 不生成草稿。
- 不发消息。

用户仍可手动使用 JD 分析、岗位列表和面试准备等非自动沟通功能。

### 6.2 草稿模式

允许：

- 采集和分析 HR 回复。
- 生成待发送消息。
- 放入待发送队列。

必须用户确认后才发送。

### 6.3 自主询问模式

允许 Agent 在规则范围内自动问岗位相关问题。

默认限制：

- 最多自动追问 2 轮。
- 超过 2 轮后暂停，等待用户选择是否继续。
- 只能围绕岗位、求职和面试流程沟通。
- 不回答与岗位无关的问题。

允许自主询问的内容：

- 工作内容。
- 技术栈。
- 实习周期。
- 每周到岗天数。
- 是否有导师。
- 是否接受大二。
- 薪资范围。
- 远程、线下或混合办公。
- 面试流程。

强制转人工：

- 联系方式。
- 面试时间。
- 发简历或上传简历。
- 薪资谈判。
- 线下地址确认。
- 身份证、银行卡等敏感信息。
- 押金、培训费、贷款等风险内容。
- 明显闲聊。
- 与岗位无关的问题。

## 7. 待发送队列

每条待发送消息至少包含：

- 岗位。
- 平台。
- 公司。
- HR 或会话标识。
- 生成原因。
- 消息正文。
- 模型和生成时间。
- 风险提示。

支持操作：

- 发送。
- 编辑后发送。
- 驳回。
- 标记为需要我处理。
- 归档岗位。

自动发送上限：

- 默认每天最多自动发 5 条第一轮询问。
- 默认每个平台每天最多自动发 3 条第一轮询问。
- 两个上限都允许用户手动修改。

Agent 自动发出的完整消息需要记录到岗位详情或沟通日志中。

## 8. HR 消息采集和巡检

### 8.1 手动对话采集

用户打开某个平台聊天页，浏览器扩展采集当前打开的 HR 对话。

聊天页采集范围：

- 采集当前打开的 HR 对话中已加载的全部消息文本。
- 不采集截图、Cookie、本地存储、请求头、隐藏字段或其他会话内容。
- 不批量采集所有 HR 聊天。
- 默认将当前对话已加载消息全部提供给 LLM，帮助避免重复询问和重复回答。
- 如果单个对话超过上下文或成本限制，则自动压缩为：岗位摘要 + 最近消息 + 历史摘要。

### 8.2 页面实时监听

用户打开招聘平台聊天页时，扩展可以监听当前页面的新消息。

前提：

- 电脑开着。
- 本地应用运行。
- 浏览器页面保持登录并打开。

### 8.3 受控自动巡检

后续可由 Playwright 使用已登录的专用 profile 定时巡检消息页。

默认巡检频率：

- 活跃找实习：60 秒一次。
- 普通模式：5 分钟一次。
- 高频临时模式：30 秒一次。
- 夜间或暂停：不巡检。

成本规则：

- 巡检本身只做浏览器读取和本地差分，不调用 LLM。
- 没有新消息时，token 消耗为 0。
- 只有检测到新 HR 消息时才触发 LLM。
- LLM 输入只包含新增消息、岗位摘要、必要上下文和候选人画像摘要。
- 页面长文本不进入消息巡检 prompt。
- 每个会话设置 cooldown，避免同一消息反复调用。

## 9. 简历和个人信息边界

允许 Agent 引用：

- 公开 GitHub。
- 公开项目链接。
- 专业。
- 年级。
- 当前求职方向。
- 候选人画像中明确填写的技能和项目事实。

具体学校名称只在候选人画像中明确填写后使用。

每周到岗天数、实习周期、城市偏好等信息：

- 不主动一上来全部发送。
- 优先询问对方岗位要求。
- 如果 HR 问到，可以按候选人画像回答。

技术能力回答规则：

- 候选人画像里有的，用“了解”“较熟悉”“有项目实践”等不夸大的词。
- 候选人画像里没有的，可以说“正在学习”或转给用户确认。
- 不使用“精通”等夸大措辞。

永远需要用户确认：

- 发送或上传简历。
- 发送简历链接。
- 点击投递。
- 发送手机号、邮箱、微信。
- 发送身份证、银行卡等敏感信息。
- 确认面试时间。
- 接受 offer。
- 填写真实个人信息表单。

## 10. 模型调用和成本控制

模型路由采用配置优先和项目内测评决定。

当前候选模型：

- `deepseek-v4-flash`：主力候选，适合高频任务。
- `deepseek-v4-pro`：复杂判断和复核候选，但不预设一定优于 Flash。
- `gpt-5.5`：高价值、复杂、敏感或面试相关任务候选。
- `gpt-5.6 terra`：高价值、复杂、敏感或面试相关任务候选。
- 本地 Ollama DeepSeek 小模型：只作为低风险预处理候选，实测不稳定时默认不用。

观察期需要做项目内评测：

- 用同一批真实 JD 跑不同模型。
- 用同一批 HR 回复跑不同模型。
- 用风险样例对比误判率。
- 记录准确性、稳定性、速度和 token 成本。
- 再决定最终模型路由和分数阈值。

推荐初始分工：

| 任务 | 默认模型 | 升级或复核模型 |
| --- | --- | --- |
| 页面类型判断 | 本地规则 | DeepSeek Flash |
| 搜索关键词扩展 | DeepSeek Flash | 无 |
| 候选去重和低质量过滤 | 本地规则 | DeepSeek Flash |
| JD 结构化抽取 | DeepSeek Flash | DeepSeek Pro |
| 岗位初评分 | 规则 + DeepSeek Flash | DeepSeek Pro |
| 公司风险摘要 | DeepSeek Flash | DeepSeek Pro |
| HR 新消息分类 | 本地规则 + DeepSeek Flash | DeepSeek Pro |
| 自动追问话术 | DeepSeek Flash | DeepSeek Pro |
| 即将自动发送的消息 | DeepSeek Pro | GPT-5.5 / GPT-5.6 terra |
| 面试准备 | DeepSeek Pro | GPT-5.5 / GPT-5.6 terra |
| 面试复盘 | DeepSeek Pro | GPT-5.5 / GPT-5.6 terra |
| 定时巡检 | 不调用模型 | 有新消息才调用 Flash |

硬规则：

- 浏览器巡检、页面差分、消息去重默认不调用 LLM。
- 高频任务优先用本地规则、Ollama 或 DeepSeek Flash。
- 涉及自动沟通、风险判断、边界判断时，用 DeepSeek Pro 复核。
- GPT-5.5 / GPT-5.6 terra 只用于高价值、复杂、敏感或面试相关任务。
- 不把完整网页和完整聊天历史反复发送给模型。
- 自动沟通使用“低成本模型先判断，高能力模型复核”的机制。
- 出现投简历、面试、薪资、身份证、押金、培训费等关键词时，必须升级复核或转人工。

日志保存：

- 任务类型。
- 平台。
- 模型。
- token。
- 费用估算。
- 结果摘要。
- 决策结果。
- 错误信息。

默认不保存完整 prompt、input、output。

调试模式下才允许临时保存完整输入输出，并且用户可以关闭和清理。

未来可添加每日成本阈值：

- 超过阈值后暂停自动回复。
- 只继续只读采集和本地差分。
- 阈值由用户自行设置，不内置强制默认值。

## 11. 面试状态和准备闭环

面试相关状态：

- 已沟通
- 面试邀请
- 待面试
- 面试准备中
- 已面试
- 面试复盘

状态含义：

- 面试邀请：HR 提到面试、笔试或约时间，但时间、形式或流程尚未确认。
- 待面试：用户已经和 HR 确认时间、形式和基本流程。
- 面试准备中：Agent 已开始生成或维护准备材料。
- 已面试：面试结束，等待结果或准备复盘。
- 面试复盘：记录没答好的问题和后续加强计划。

识别到面试邀请后：

- 标记岗位为“面试邀请”。
- 不自动回复面试时间。
- 不自动生成面试准备材料。
- 提醒用户去确认时间、形式和要求。

用户手动改为“待面试”后：

- Agent 开始生成面试准备。
- 生成 JD 技术点拆解。
- 生成项目可能追问。
- 生成 3 天冲刺计划。
- 生成 7 天准备计划。
- 生成模拟面试题。
- 生成不夸大的项目介绍。

线上面试支持边界：

- 先做面试前准备、模拟面试和面试后复盘。
- 不做实时替答或实时作弊式辅助。
- 后续可接入本地 ASR 做录音转写复盘。

面试复盘需要保留“没答好的问题库”，用于后续加强模拟面试。

## 12. 阶段路线

### V0.2 浏览器扩展采集 MVP

目标：

- 在用户正常登录的 Edge/Chrome 中采集当前页面。
- 不重复登录。
- 不自动沟通。
- 不投递。

范围：

- 新增本地采集接口。
- 新增浏览器扩展目录。
- 扩展按钮：采集当前岗位。
- 扩展按钮：采集搜索结果。
- 采集当前岗位后创建岗位并进入分析。
- 采集搜索结果后创建候选列表，不逐条抓 JD。

安装方式：

- 第一版使用开发者模式加载本地 `browser_extension/` 目录。
- 后续再考虑打包。

### V0.3 对话采集与草稿模式

目标：

- 用户打开 HR 对话页。
- 扩展采集当前对话。
- Agent 分析 HR 回复。
- 生成待发送队列。
- 用户确认后发送。

不做：

- 自动回复。
- 自动巡检全部会话。
- 自动投递。

### V0.4 自主询问模式

目标：

- 全局开启自主询问。
- Agent 自动问岗位相关问题。
- 默认最多 2 轮。
- 敏感、非岗位、面试时间、简历投递等内容转人工。

### V0.5 受控自动搜索与消息巡检

目标：

- 用户点击“寻找岗位”。
- Agent 跨平台搜索。
- 每个平台最多 20 个候选。
- 每轮总候选最多 100 个。
- 定时消息巡检。
- 有新消息才调用 LLM。

### V0.6 受控自动投递准备

目标：

- 高匹配、低风险岗位进入投递候选。
- Agent 整理投递理由。
- Agent 推荐简历版本。
- 用户确认后才投递或发简历。

### V0.7 面试闭环增强

目标：

- 面试邀请识别。
- 待面试后自动准备。
- 模拟面试。
- 面试复盘。
- 没答好问题库。
- 后续本地 ASR。

## 13. 当前下一步

V0.2 浏览器扩展采集 MVP 已实现。V0.3 对话采集与草稿模式 MVP 已实现。
V0.3 真实平台校准入口已实现：对话采集会保存原始可见文本、清洗后文本、过滤行和用户反馈。
V0.4 第一块已实现：全局沟通模式开关和自主询问轮次上限。当前仍不会自动发送消息。
V0.4 第二块已实现：基础行为审计日志，可回看对话采集、草稿处理、分类反馈和沟通设置变更。
V0.4 第三块已实现：自动化暂停 / 恢复与对话差分跳过。当前差分只用于手动扩展对话采集和未来巡检前的本地预过滤，还没有实现定时巡检。

当前优先做 V0.3 真实平台验证：

1. 在真实 Boss 直聘、实习僧等 HR 对话页测试“采集当前对话”。
2. 校准当前页面可见文本清洗规则。
3. 校准 HR 回复分类：岗位沟通、面试邀请、需要我处理、无关内容。
4. 校准草稿话术，不夸大候选人能力。
5. 确认“面试邀请”状态和后续“待面试”手动切换流程。
6. 将误判样本在“沟通草稿”页标记为正确、误判或待观察，并记录期望分类。

下一阶段是 V0.4 自主询问模式。

第一批实现任务：

1. 已完成：全局沟通模式开关：关闭、草稿模式、自主询问模式。
2. 已完成：待发送队列保留人工确认和编辑入口。
3. 已完成第一版：自主询问只允许岗位相关问题，沿用现有安全分类和转人工规则。
4. 已完成第一版：默认最多自动追问 2 轮，达到上限后转人工。
5. 已完成第一版：敏感、非岗位、面试时间、简历投递等内容转人工。
6. 已完成第一版：全局暂停 / 恢复控制，暂停时对话采集不分析、不生成草稿。
7. 已完成第一版：对话本地差分，重复采集无新增内容时不调用 LLM。

暂不做：

- 自动发送消息。
- 自动投递。
- 定时巡检。
- 录音 ASR。

## 14. 参考资料

- Chrome Side Panel API: https://developer.chrome.com/docs/extensions/reference/api/sidePanel
- Chrome Content Scripts: https://developer.chrome.com/docs/extensions/develop/concepts/content-scripts
- Chrome Native Messaging: https://developer.chrome.com/docs/extensions/develop/concepts/native-messaging
- Microsoft Edge Native Messaging: https://learn.microsoft.com/en-us/microsoft-edge/extensions/developer-guide/native-messaging
- Playwright BrowserType / CDP: https://playwright.dev/python/docs/api/class-browsertype
